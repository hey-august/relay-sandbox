"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createDestroyableSlice = void 0;
const createSlice_1 = require("../toolkit/createSlice");
const actions_1 = require("../actions");
const createDestroyableSlice = ({ name = '', initialState, reducers, extraReducers, }) => {
    return (0, createSlice_1.createSlice)({
        name,
        initialState,
        reducers,
        extraReducers: (builder) => {
            builder.addCase(actions_1.destroyAction.type, () => {
                return initialState;
            });
            if (typeof extraReducers === 'function') {
                extraReducers(builder);
            }
        },
    });
};
exports.createDestroyableSlice = createDestroyableSlice;
//# sourceMappingURL=createDestroyableSlice.js.map