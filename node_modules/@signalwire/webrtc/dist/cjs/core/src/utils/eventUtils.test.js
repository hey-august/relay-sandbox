"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const eventUtils_1 = require("./eventUtils");
describe('eventUtils', () => {
    describe('stripNamespacePrefix', () => {
        it('should strip first word before dot', () => {
            const event1 = 'random.event.foo';
            const event2 = 'random.event.bar';
            expect((0, eventUtils_1.stripNamespacePrefix)(event1)).toBe('event.foo');
            expect((0, eventUtils_1.stripNamespacePrefix)(event2)).toBe('event.bar');
        });
        it('should not strip if there is no dot', () => {
            const event1 = 'randomeventfoo';
            const event2 = 'randomeventbar';
            expect((0, eventUtils_1.stripNamespacePrefix)(event1)).toBe('randomeventfoo');
            expect((0, eventUtils_1.stripNamespacePrefix)(event2)).toBe('randomeventbar');
        });
        it('should strip the namespace if passed', () => {
            const event1 = 'video.event.foo';
            const event2 = 'voice.event.bar';
            expect((0, eventUtils_1.stripNamespacePrefix)(event1, 'video')).toBe('event.foo');
            expect((0, eventUtils_1.stripNamespacePrefix)(event2, 'voice')).toBe('event.bar');
        });
    });
});
//# sourceMappingURL=eventUtils.test.js.map