import { createSlice, } from '../toolkit/createSlice';
import { destroyAction } from '../actions';
export const createDestroyableSlice = ({ name = '', initialState, reducers, extraReducers, }) => {
    return createSlice({
        name,
        initialState,
        reducers,
        extraReducers: (builder) => {
            builder.addCase(destroyAction.type, () => {
                return initialState;
            });
            if (typeof extraReducers === 'function') {
                extraReducers(builder);
            }
        },
    });
};
//# sourceMappingURL=createDestroyableSlice.js.map