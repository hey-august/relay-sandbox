export const getIceServers = ({ session }) => {
    var _a;
    return (_a = session === null || session === void 0 ? void 0 : session.iceServers) !== null && _a !== void 0 ? _a : [];
};
export const getSession = (store) => {
    return store.session;
};
export const getAuthStatus = ({ session }) => {
    return session.authStatus;
};
export const getAuthError = ({ session }) => {
    return session.authError;
};
export const getAuthState = ({ session }) => {
    return session.authState;
};
//# sourceMappingURL=sessionSelectors.js.map