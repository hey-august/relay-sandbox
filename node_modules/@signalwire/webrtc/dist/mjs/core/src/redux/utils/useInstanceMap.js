export const useInstanceMap = () => {
    // Generic map stores multiple instance
    // For eg;
    // callId => CallInstance
    // controlId => PlaybackInstance | RecordingInstance
    const instanceMap = new Map();
    const getInstance = (key) => {
        return instanceMap.get(key);
    };
    const setInstance = (key, value) => {
        instanceMap.set(key, value);
        return instanceMap;
    };
    const deleteInstance = (key) => {
        instanceMap.delete(key);
        return instanceMap;
    };
    return {
        get: getInstance,
        set: setInstance,
        remove: deleteInstance,
    };
};
//# sourceMappingURL=useInstanceMap.js.map