export const getComponent = ({ components }, id) => {
    var _a;
    return (_a = components.byId) === null || _a === void 0 ? void 0 : _a[id];
};
export const getComponentsById = ({ components }) => {
    return components.byId;
};
export const getComponentsToCleanup = (state) => {
    const components = getComponentsById(state);
    let toCleanup = [];
    Object.keys(components).forEach((id) => {
        if (components[id].responses || components[id].errors) {
            toCleanup.push(id);
        }
    });
    return toCleanup;
};
//# sourceMappingURL=componentSelectors.js.map