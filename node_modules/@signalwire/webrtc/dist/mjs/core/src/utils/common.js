const UPPERCASE_REGEX = /[A-Z]/g;
/**
 * Converts values from camelCase to snake_case
 * @internal
 */
export const fromCamelToSnakeCase = (event) => {
    // @ts-ignore
    return event.replace(UPPERCASE_REGEX, (letter) => {
        return `_${letter.toLowerCase()}`;
    });
};
export const WEBRTC_EVENT_TYPES = [
    'webrtc.message',
    // 'webrtc.verto',
];
export const isWebrtcEventType = (eventType) => {
    // @ts-expect-error
    return WEBRTC_EVENT_TYPES.includes(eventType);
};
//# sourceMappingURL=common.js.map