import { PRODUCT_PREFIX_VIDEO } from '../utils/constants';
import { toExternalJSON } from '../utils';
/**
 * Used to not duplicate member fields across constants and types
 * and generate `MEMBER_UPDATED_EVENTS` below.
 * `key`: `type`
 */
export const INTERNAL_MEMBER_UPDATABLE_PROPS = {
    audio_muted: true,
    video_muted: true,
    deaf: true,
    visible: true,
    input_volume: 1,
    output_volume: 1,
    input_sensitivity: 1,
};
export const INTERNAL_MEMBER_UPDATED_EVENTS = Object.keys(INTERNAL_MEMBER_UPDATABLE_PROPS).map((key) => {
    return `${PRODUCT_PREFIX_VIDEO}.member.updated.${key}`;
});
export const MEMBER_UPDATABLE_PROPS = toExternalJSON(INTERNAL_MEMBER_UPDATABLE_PROPS);
export const MEMBER_UPDATED_EVENTS = Object.keys(MEMBER_UPDATABLE_PROPS).map((key) => {
    return `member.updated.${key}`;
});
//# sourceMappingURL=videoMember.js.map