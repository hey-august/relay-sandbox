import { MakeRoomOptions } from '../Client';
import { RoomSession } from '../video';
declare type Strategy = 'room';
declare type StrategyParams = RoomStrategyParams;
declare type RoomStrategyParams = {
    token: string;
};
interface FabricCallResponse {
    strategy: Strategy;
    params: StrategyParams;
    userParams: MakeRoomOptions;
}
export declare const buildCall: ({ strategy, params, userParams, }: FabricCallResponse) => RoomSession;
export {};
//# sourceMappingURL=buildCall.d.ts.map